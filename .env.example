# Optional Supabase backend (recommended for shared multi-device usage)
# Use the WORDLE-prefixed variables to avoid conflicts with other projects.
VITE_WORDLE_SUPABASE_URL=
VITE_WORDLE_SUPABASE_ANON_KEY=

# Optional: prefill league code on first load
VITE_DEFAULT_LEAGUE_CODE=
